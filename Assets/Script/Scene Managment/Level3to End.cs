using UnityEngine;

public class Level3toEnd : MonoBehaviour
{
  // Public BossHealth bossHealth
//when boss is killed
    // Update is called once per frame
    void Update()
    {
        // if(bossHealth.Health == 0) { killed boss SceneTransitionManager.Instance.TransitionToScene("post Game");}
    }
}
